import { Box } from '@mui/material';
import BasicDetails from './BasicDetails';

const Profile = () => {
    return (
        <Box>
            <BasicDetails />
        </Box>
    );
};

export default Profile;
