import { Box } from '@mui/material';

const ProfilePhoto = () => {
    return (
        <Box
            sx={{
                flex: '1',
            }}
        >
            Profile Photo
        </Box>
    );
};

export default ProfilePhoto;
