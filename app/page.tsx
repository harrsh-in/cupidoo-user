'use client';

import { Button, Container } from '@mui/material';

const Home = () => {
    return (
        <Container>
            <Button variant="outlined">Press me</Button>
        </Container>
    );
};

export default Home;
